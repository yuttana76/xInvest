export const APP_NAME = process.env.NEXT_PUBLIC_APP_NAME || 'xInvest';
export const APP_TAGLINE = process.env.NEXT_PUBLIC_APP_TAGLINE || 'Smart Investment Intelligence';

// Used by the split-letter logo mark (badge + rest), e.g. "x" + "Invest".
export const APP_NAME_BADGE = APP_NAME.charAt(0);
export const APP_NAME_REST = APP_NAME.slice(1);
